name: Deploy Apps Script

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'personal-assistant/google-apps-script/**'
      - 'scripts/validate-gas-folders.mjs'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/deploy-apps-script.yml'

jobs:
  push:
    name: clasp push
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate Apps Script folders
        run: npm run validate:gas

      - name: Configure clasp auth
        env:
          CLASPRC_JSON: ${{ secrets.CLASPRC_JSON }}
          CLASP_TOKEN: ${{ secrets.CLASP_TOKEN }}
        run: |
          auth_json="${CLASPRC_JSON:-${CLASP_TOKEN:-}}"
          if [ -z "$auth_json" ]; then
            echo "::error::Missing clasp auth secret. Set CLASPRC_JSON or CLASP_TOKEN to the full ~/.clasprc.json contents."
            exit 1
          fi
          printf '%s' "$auth_json" > "$HOME/.clasprc.json"
          chmod 600 "$HOME/.clasprc.json"
          node -e "const fs = require('fs'); const path = process.env.HOME + '/.clasprc.json'; try { JSON.parse(fs.readFileSync(path, 'utf8')); } catch (error) { console.error('::error::clasp auth secret is present but is not valid JSON. Store the full ~/.clasprc.json contents in CLASPRC_JSON or CLASP_TOKEN.'); process.exit(1); }"

      - name: Push Apps Script projects
        shell: bash
        run: |
          set -euo pipefail

          folders=(
            "personal-assistant/google-apps-script/control-center"
            "personal-assistant/google-apps-script/hub"
            "personal-assistant/google-apps-script/registry"
            "personal-assistant/google-apps-script/automation"
            "personal-assistant/google-apps-script/dashboard"
          )

          for folder in "${folders[@]}"; do
            script_id="$(node -e "const fs=require('fs'); const c=JSON.parse(fs.readFileSync(process.argv[1], 'utf8')); process.stdout.write(c.scriptId || '')" "$folder/.clasp.json")"

            if [[ "$script_id" == REPLACE_WITH_* ]]; then
              echo "::warning file=$folder/.clasp.json::Skipping $folder because scriptId is still a placeholder."
              continue
            fi

            echo "Pushing $folder"
            (cd "$folder" && npx clasp push --force)
          done
